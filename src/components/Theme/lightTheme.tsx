import type { Theme } from "./types"

const lightTheme: Theme = {
    colors: {
        background: "white",
        error: "red",
        primary: "blue",
        success: "green"
    }
}

export default lightTheme